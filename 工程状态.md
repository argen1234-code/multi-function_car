# 工程状态 — 2026-06-21

## 已完成

### TF 卡 / FATFS / 图片加载

| 任务 | 说明 |
|---|---|
| SDMMC1 驱动 | PC8/9/10/11/12 + PD2, 4-bit 20MHz |
| FATFS R0.12 | `system/ff.c`, 支持双卷 (0:SD, 1:QSPI) |
| 图片从 TF 卡加载 | FAT32 → f_read → SDRAM → LVGL, 支持大小图 |
| `__disable_irq` 修复大文件读取 | polling 模式关中断, 解决 >250KB 读取失败 |
| `bsp_tf_image_load.c/h` | 图片加载封装 |
| `bsp_fatfs_diskio.c` | 双卷 disk I/O 适配 |

### QSPI Flash

| 任务 | 说明 |
|---|---|
| W25Q64 底层驱动 | QSPI 接口: PF10/PG6/PF8/PF9/PF7/PF6, 120MHz |
| 读写 + 擦除验证通过 | raw 256B 读写 PASS |
| HAL QSPI 模块 | `stm32h7xx_hal_qspi.c` 已加入 |

### 文件管理

| 任务 | 说明 |
|---|---|
| `bsp_fatfs_ex.c/h` | 文件类型识别、磁盘空间查询、文件夹/文件复制(带进度回调) |

### 引脚重分配

| 任务 | 说明 |
|---|---|
| PC4/PC5 替代 PC11/PC12 | 前右轮电机方向引脚释放给 SDMMC1 |
| UART4 禁用 | PC10 释放给 SDMMC1_D2 |

### 系统 / 编译

| 任务 | 说明 |
|---|---|
| MicroLib + printf 重定向 | `fputc` → USART1 (bsp_uart.c) |
| 中文 UTF-8 转义 | `cn_to_utf8_escapes.pl` |
| 图片数据 strip | `strip_image_data.pl` |
| LV_MEM_SIZE → 8MB | SDRAM 池扩容 |
| `CLAUDE.md` / `PCB_Pinout.md` 更新 | 注释 + 引脚文档 |
| SquareLine 后处理流程 | 完整的移植脚本链 |

### 工具（`reference/` 目录）

| 工具 | 用途 |
|---|---|
| LvglFont | 4bpp 抗锯齿字体生成器 |
| Lvgl_image_convert | 图片格式转换 |
| GIF 图片生成器 | GIF → LVGL 帧 |
| W25QXX 驱动源码 | 商家原版 QSPI 驱动 |
| QSPI 历程 | 读写 + 内存映射两个例程 |
| 07 图片显示实验 | FATFS+W25QXX+SD 卡完整实验 |
| GUI_Designs/ | 参考 UI 页面 (12 个页面 + 登录 + 数据显示) |
| L610*.pdf | 4G Cat.1 模块文档 |
| 硬件.docx | 硬件设计文档 |

---

## 待完成

### 高优先级

| 任务 | 说明 |
|---|---|
| **FATFS-on-QSPI** | `f_mkfs` 格式化卡死, 底层读写已验证通过, 需调试 disk_write 页写入逻辑 |
| **大图片 >250KB** | 当前 `icon.bin`(12K) + `bg.bin`(1.8M) 均可加载, 但 `__disable_irq` 方案使系统 tick 暂停 ~200ms |

### 中优先级

| 任务 | 说明 |
|---|---|
| L610 4G Cat.1 模块 | 远程遥测/控制, AT 指令 UART 通信 |
| Modbus RTU | 工业传感器扩展 (参考 `reference/` 多个源码) |
| LvglFont 4bpp 字体 | 替换当前 1bpp 锯齿中文 |
| LVGL 文件系统 (`LV_USE_FS_FATFS`) | 让 LVGL 直接 `lv_img_set_src(obj, "S:bg.bin")` |
| LV_MEM_SIZE 8MB 为何不生效 | 运行时分配 2.4MB 仍然失败, 怀疑编译缓存或 lv_mem_init 未更新 |

### 低优先级

| 任务 | 说明 |
|---|---|
| ~~登录+主页 UI~~ | ✅ 已完成 |
| ~~数据显示 UI~~ | ✅ 已完成 (DataDisplay + DataDetail) |
| ~~LVGL 文件系统~~ | ✅ 已完成 (FileManager) |
| QSPI 内存映射模式 | 只读但极快 (~60MB/s), 适合存图片 |
| RS485 驱动 | 已有参考代码 |
| DHT11 温湿度 | 已有参考代码 |

---

## Keil 工程分组速查

| 组 | 文件 |
|---|---|
| **system** | `ff.c` |
| **Application/User/Core** | `sdmmc_sd.c` |
| **Hardware** | `bsp_TF_card.c`, `bsp_fatfs_diskio.c`, `bsp_tf_image_load.c`, `bsp_qspi_flash.c`, `bsp_fatfs_ex.c` |
| **BSP_LVGL** | `ui_img_1301672413.c`, `ui_img_1943878613.c`, `ui_img_380527393.c`, `ui_img_1565185032.c`, `ui_img_1614311220.c`, `ui_Login.c`, `ui_FileManager.c`, `ui_DataDisplay.c`, `ui_DataDetail.c` |
| **HAL** | `stm32h7xx_hal_sd.c`, `stm32h7xx_ll_sdmmc.c`, `stm32h7xx_hal_qspi.c` |

---

## 功能来源分类

### 从参考工程/SDK 直接移植

| 功能 | 来源 |
|---|---|
| FATFS 核心（ff.c/h/ffconf.h/diskio.h/integer.h） | SDK 例程 `05.SDIO-基本数据读写` |
| SDMMC SD 驱动（sdmmc_sd.c/h） | 同上 |
| QSPI W25Q64 驱动（bsp_qspi_flash.c/h） | SDK 例程 `13.QSPI-简单读写测试` |
| 文件管理工具（bsp_fatfs_ex.c/h） | 参考 `LVGL综合实验/Middlewares/FATFS/exfuns` |
| `__disable_irq` 大文件读取修复 | 参考 `sdio_sdcard.c` 注释 |
| `reference/` 目录下所有参考代码 | 挑战杯资料 |

### SquareLine Studio 生成后移植

| 文件 | 说明 |
|---|---|
| ui.c / ui.h | 主 UI 入口（主题、屏幕注册） |
| ui_helpers.c/h | 动画/属性辅助 |
| ui_events.h | 事件定义 |
| ui_Screenmain.c/h | 主页 |
| ui_Screensettings.c/h | 设置页 |
| ui_font_Font1.c / FontTitle.c | 中文字体 |
| ui_img_1301672413.c / 1943878613.c | 图片描述符（已 strip 为占位符） |

### 手写/重写

| 文件 | 参考来源 |
|---|---|
| ui_Login.c/h | 登录页（密码 qmq），参考「登录加主页」设计概念 |
| ui_FileManager.c/h | 文件浏览器（可进文件夹、滑动列表），参考 `lv_file.c` 重写 |
| ui_DataDisplay.c/h | 数据面板（GPS/Speed/IMU/Mode 四格），参考「数据显示」概念 |
| ui_DataDetail.c/h | 详情页（tabview + mode roller），参考 `tileview` 重写 |
| bsp_TF_card.c/h | TF 卡封装，参考 SD 例程 API |
| bsp_tf_image_load.c/h | 图片从 TF 卡 FAT32 加载 |
| bsp_fatfs_diskio.c | 双卷 diskio（SD+QSPI），参考 `diskio.c` 重写 |
| 60 秒自动登出 + 全局触摸重置 | 原创 |

---

## TF 卡文件

```
TF 卡 (FAT32):
  ├── icon.bin    (64×64 RGBA, 12KB)
  └── bg.bin      (1024×600 RGBA, 1.8MB)
```
